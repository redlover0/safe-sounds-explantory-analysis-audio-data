const csv = require('csv-parser');
const fs = require('fs');
const Chart = require('cli-chart');       
const prompt = require('prompt');         


const SafeSoundMenu = async () => {
    console.log("_".repeat(50))
    console.log("\n Welcome to SafeSound, creating advanced audio analytics to meet intellegent amplifcation, setting a new standards on using science of sound")
    console.log("_".repeat(50))
};
